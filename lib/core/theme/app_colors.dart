import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Dark Theme Background & Surfaces
  static const Color background = Color(0xFF0F0F12);
  static const Color surface = Color(0xFF18181C);
  static const Color surfaceCard = Color(0xFF202026);
  static const Color surfaceHover = Color(0xFF2A2A32);
  static const Color border = Color(0xFF2E2E38);

  // Vibrant Accents (Square POS / High contrast Dark UI)
  static const Color accentLime = Color(0xFFD0F244);
  static const Color accentYellow = Color(0xFFFFD600);
  static const Color accentGreen = Color(0xFF34C759);
  static const Color accentBlue = Color(0xFF0A84FF);
  static const Color accentOrange = Color(0xFFFF9F0A);
  static const Color accentRed = Color(0xFFFF453A);

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9898A0);
  static const Color textMuted = Color(0xFF63636E);

  // Status colors
  static const Color success = Color(0xFF32D74B);
  static const Color warning = Color(0xFFFF9F0A);
  static const Color danger = Color(0xFFFF453A);
}
